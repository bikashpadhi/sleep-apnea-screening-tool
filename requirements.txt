# ─────────────────────────────────────────────────────────────────────────────
# Sleep Apnea Screening — Python dependencies
#
# Install with:
#     pip install -r requirements.txt
#
# GPU (CUDA 12.x) users should install PyTorch separately first:
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# ─────────────────────────────────────────────────────────────────────────────

# Core deep learning
torch>=2.2.0
torchvision>=0.17.0

# Signal processing & EDF reading
mne>=1.7.0
scipy>=1.13.0

# Data processing
numpy>=1.26.0
pandas>=2.2.0

# ML utilities
scikit-learn>=1.5.0

# Visualisation
matplotlib>=3.9.0
seaborn>=0.13.0

# Notebook support
jupyter>=1.0.0
ipykernel>=6.29.0

# Optional — progress bars in training loop
tqdm>=4.66.0
